import { FilmGenreFilterPipe } from './film-genre-filter.pipe';

describe('FilmGenreFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilmGenreFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
