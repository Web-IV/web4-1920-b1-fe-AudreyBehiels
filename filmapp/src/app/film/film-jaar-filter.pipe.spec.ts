import { FilmJaarFilterPipe } from './film-jaar-filter.pipe';

describe('FilmJaarFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilmJaarFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
